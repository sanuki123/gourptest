name: aaa3

on:
  push:
    branches:
      - main

  workflow_dispatch:
  
jobs:

  call-workflow3:
    concurrency:
      group: AAA
      cancel-in-progress: false
  
    -
      run: |
        echo ${{ github.ref }}
