name: aaa3

on:
  push:
    branches:
      - main

  workflow_dispatch:
  
jobs:

  aaajob3:
    runs-on: ubuntu-latest

    concurrency:
      group: AAA
      cancel-in-progress: false
  
    -
      run: |
        echo ${{ github.ref }}
